= content_for :title, (t 'createad')
.form-wrapper

  %h3.text-center.mb-5 
    =t 'publishad'

  = render 'form', buttoncaption: (t 'publishmyad'), debouncecaption: (t 'publishing')

.text-center
  = link_to (t 'back'), :back
