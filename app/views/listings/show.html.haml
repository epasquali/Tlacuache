.listing-wrapper

  .card.mt-3
    .card-header.bg-lightgrey.mb-0
      %h5.card-title.text-center
        = @listing.title
            
    .card-body
      .text-justify
        = @listing.description
      -if @listing.image.attached?
        =image_tag @listing.image, class: 'img-fluid mt-4'


.text-center
  = link_to 'Editar', edit_listing_path(@listing)
  \|
  = link_to 'Regresar', listings_path
