= render 'layouts/carousel'

.container-fluid.px-5

  = render 'layouts/listingnavtabs'

- if current_user
  %h3= current_user.name
  %h3= current_user.email
  = image_tag(current_user.image) unless current_user.image.nil?


